@model IEnumerable<FirstWebProject.Models.ProcentBase>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.bank_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.program_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.procent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.bank_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.program_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.procent)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
